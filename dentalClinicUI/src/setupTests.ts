

import '@testing-library/jest-dom';

// If you want tests to default to English:
// beforeEach(() => {
//     vi.spyOn(Storage.prototype, 'getItem')
//         .mockImplementation((k: string) => (k === 'lang' ? 'en' : null));
// });
// afterEach(() => vi.restoreAllMocks());

